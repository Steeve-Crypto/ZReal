apiVersion: v1
kind: Service
metadata:
  name: {{ include "zreal.fullname" . }}
  labels:
    {{- include "zreal.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "zreal.selectorLabels" . | nindent 4 }}
